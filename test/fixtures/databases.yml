# To run tests:
#  1. Copy this file to test/connections/databases.yml.
#  2. Update to match the databases you want to test against.

source:
  host: localhost
  dbname: pgdiff_source
  user: postgres

target:
  host: localhost
  dbname: pgdiff_target
  user: postgres
